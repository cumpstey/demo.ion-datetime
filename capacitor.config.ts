import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'demo.ion-datetime',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
